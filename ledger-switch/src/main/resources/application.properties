server.port=9090
spring.application.name=ledger-switch

# ==========================================
# ? DATABASE CONFIGURATION (Read/Write Split)
# ==========================================

# ? WRITER (Primary) - Used for @Transactional
spring.datasource.writer.jdbc-url=jdbc:postgresql://ledger-master-db.cjiaa0ye444g.ap-south-1.rds.amazonaws.com:5432/switch_db
spring.datasource.writer.username=postgres
spring.datasource.writer.password=Ledger-master-db123
spring.datasource.writer.driver-class-name=org.postgresql.Driver
spring.datasource.writer.hikari.maximum-pool-size=10
spring.datasource.writer.hikari.minimum-idle=5
spring.datasource.writer.hikari.pool-name=HikariPool-Switch-Writer

# ? READER (Replica) - Used for @Transactional(readOnly=true)
# Note: I updated the URL to point to 'ledger-replica-db' based on your previous config
spring.datasource.reader.jdbc-url=jdbc:postgresql://ledger-replica-db.cjiaa0ye444g.ap-south-1.rds.amazonaws.com:5432/switch_db
spring.datasource.reader.username=postgres

# Monitoring
management.endpoints.web.exposure.include=health,info,prometheus
management.endpoint.health.show-details=always
management.metrics.tags.application=${spring.application.name}
spring.datasource.reader.password=Ledger-master-db123
spring.datasource.reader.driver-class-name=org.postgresql.Driver
spring.datasource.reader.hikari.maximum-pool-size=20
spring.datasource.reader.hikari.minimum-idle=5
spring.datasource.reader.hikari.pool-name=HikariPool-Switch-Reader

# ==========================================
# ? NEO4J & REDIS CONFIGURATION
# ==========================================
spring.neo4j.uri=bolt://pythonapi.ledgerzero.xyz:7687
spring.neo4j.authentication.username=neo4j
spring.neo4j.authentication.password=Manil@81125

# ==========================================
# JPA / Hibernate
# ==========================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Disable default auto-config to allow our custom DataSourceConfig to work
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration

# ==========================================
# SERVICE LINKS
# ==========================================
app.urls.bank.axis=http://bank-axis:8070
app.urls.bank.sbi=http://bank-sbi:8071
spring.data.redis.url=${REDIS_URL}
